plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    // OCR 라이브러리 (이거 한 줄이면 끝납니다)
    implementation 'net.sourceforge.tess4j:tess4j:5.4.0'

    // 로그 관련 에러 방지용
    implementation 'org.slf4j:slf4j-simple:2.0.7'
}